{
  "name": "ocu-eth-usd",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ urbanisierung/ocu-eth-usd",
    "create-local": "graph create --node http://localhost:8020/ urbanisierung/ocu-eth-usd",
    "remove-local": "graph remove --node http://localhost:8020/ urbanisierung/ocu-eth-usd",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 urbanisierung/ocu-eth-usd"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
